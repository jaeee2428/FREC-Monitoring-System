# Pages

This folder contains page-level React components for the frontend application.

- `LoginPage.jsx` is the dedicated login/auth entry page.
- Add future full-screen pages such as `DashboardPage.jsx` and `ProfilePage.jsx` here.
